# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Capacitor — native platform outputs are regenerated by `cap sync`
/android
/ios

# Local env / secrets (never commit a real Groq key)
.env
.env.local

# Claude Code local settings (per-user permissions, not project state)
.claude/
